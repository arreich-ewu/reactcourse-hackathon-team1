export default class OpeningTimeModel {
    constructor(day, opening, close, _id) {
      this.day = day;
      this.opening = opening;
      this.close = close;
      this._id = _id;
    }
  }